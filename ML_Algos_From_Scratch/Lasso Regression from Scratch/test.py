import numpy as np
import pandas as pd


def _linear_mode(X, theta, bias ):
    return np.dot(X, theta) + bias

def _initialize_parameters(n_features):
    theta = np.random.random_sample(n_features)
    bias = np.random.random_sample()

def _initialize_parameters(n_features):
    theta = np.random.random_sample()


